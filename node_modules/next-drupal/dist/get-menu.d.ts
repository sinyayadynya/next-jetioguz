import { AccessToken, DrupalMenuLinkContent, JsonApiWithLocaleOptions } from "./types";
export declare function getMenu<T extends DrupalMenuLinkContent>(name: string, options?: {
    deserialize?: boolean;
    accessToken?: AccessToken;
} & JsonApiWithLocaleOptions): Promise<{
    items: T[];
    tree: T[];
}>;
