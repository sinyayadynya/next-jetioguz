import { DrupalMenuLinkContent } from "./types";
export declare function useMenu<T extends DrupalMenuLinkContent>(name: string): {
    items: T[];
    tree: T[];
    error: unknown;
    isLoading: boolean;
};
